{
  "name": "hexatalk-backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy -y",
    "prod:deploy": "convex deploy -y",
    "prod:data": "convex data --prod",
    "prod:env": "convex env list --prod",
    "prod:logs": "convex logs --prod"
  },
  "dependencies": {
    "convex": "^1.42.1"
  }
}
